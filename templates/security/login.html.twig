{% extends 'base.html.twig' %}

{% block body %}

<h1>Connexion</h1>

<form action="{{ path('security_login') }}" method="post">
  <div class="form-group">
    <input placeholder="Adresse email ..." type="text" required name="_username" class="username" value="">
  </div>
  <div class="form-group">
    <input placeholder="Mot de passe ..." type="text" required name="_password" class="password" value="">
  </div>
  <div>
    <button type="submit" class="btn btn-succes">Connexion</button>
  </div>
</form>

{% endblock %}
