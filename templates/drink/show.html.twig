{% extends 'base.html.twig' %}

{% block title %}Drink{% endblock %}

{% block body %}
    <h1>Drink</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td class="txtBlackecol">{{ drink.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td class="txtBlackecol">{{ drink.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td class="txtBlackecol">{{ drink.price }}</td>
            </tr>
            <tr>
                <th>Centiliter</th>
                <td class="txtBlackecol">{{ drink.centiliter }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td class="txtBlackecol">{{ drink.image }}</td>
            </tr>
        </tbody>
    </table>

    <a class="titleBlackcol colLink linkUnderline" href="{{ path('drink_index') }}">back to list</a>

    <a class="titleBlackcol colLink linkUnderline" href="{{ path('drink_edit', {'id': drink.id}) }}">edit</a>

    {{ include('drink/_delete_form.html.twig') }}
{% endblock %}
